--modulePath
${java.home}/jmods/:${project.build.directory}/modules/:${javafx.mods}
--addModules
${java-mod-name},${modules-in-use}
--launcher
${project.artifactId}=${java-mod-name}/${main-class}
--output
${project.build.directory}/jlink-image
--strip-debug
--no-header-files
--no-man-pages
--compress=2
