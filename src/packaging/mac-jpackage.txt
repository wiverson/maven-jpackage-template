--jlink-options "--strip-native-commands --strip-debug --no-header-files --no-man-pages --compress=2"
--name TestApp
--icon ${project.basedir}/app-icon.icns
--module-path ${java.home}/jmods/:${project.build.directory}/modules/:${javafx.mods}/
--add-modules ${java-mod-name},${modules-in-use}
--module ${java-mod-name}/${main-class}
--dest ${project.build.directory}
